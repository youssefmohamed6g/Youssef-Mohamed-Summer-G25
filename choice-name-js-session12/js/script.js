var name = "youssef"
for (var i = 1; i < 15; i++) {
    var choice_name =prompt(`choice name (number ${i}) `)
    if (name == choice_name){
        alert(`you choice name in rule ${i}`)
        break
    } 
}